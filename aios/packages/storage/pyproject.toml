[project]
name = "aios-storage"
version = "0.1.0"
description = "AIOS persistence: SQLAlchemy models, append-only event store, projections"
requires-python = ">=3.11"
dependencies = ["sqlalchemy[asyncio]>=2.0", "numpy>=1.26", "alembic>=1.14", "aios-core"]

[project.optional-dependencies]
# asyncpg=アプリのasyncランタイム用 / psycopg=Alembic同期マイグレーション用 / pgvector=vector型
postgres = ["asyncpg>=0.30", "psycopg[binary]>=3.2", "pgvector>=0.3"]

[tool.uv.sources]
aios-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aios_storage"]
